import React from 'react';
// import PropTypes from 'prop-types';

const PlaybookTextEditor = () => (
  <section aria-label="Text Editor">
    <h2>Body</h2>
  </section>
);

// PlaybookTextEditor.propTypes = {};

export default PlaybookTextEditor;
